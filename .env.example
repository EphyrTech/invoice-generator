# Database Configuration
DATABASE_URL=postgresql://postgres:your_secure_password@db:5432/invoice_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=invoice_db

# Next.js Configuration
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_API_URL=https://your-domain.com/api

# NextAuth Configuration (if using authentication)
NEXTAUTH_URL=https://your-domain.com
NEXTAUTH_SECRET=your_nextauth_secret_key

# Coolify Configuration
# These will be automatically set by Coolify:
# COOLIFY_FQDN=your-app.your-domain.com
# COOLIFY_URL=https://your-app.your-domain.com

# Development/Local Configuration (not needed for Coolify)
# PgAdmin Configuration (only for local development)
# PGADMIN_DEFAULT_EMAIL=admin@example.com
# PGADMIN_DEFAULT_PASSWORD=admin
