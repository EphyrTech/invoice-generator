# =============================================================================
# PRODUCTION DEPLOYMENT (Coolify)
# =============================================================================
# For Coolify deployment, ALL variables are automatically configured in
# docker-compose.prod.yml - no manual setup required!
# Just push to GitHub and deploy.

# =============================================================================
# LOCAL DEVELOPMENT ONLY
# =============================================================================
# Database Configuration (for local development)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/invoice_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=invoice_db

# Next.js Configuration
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_API_URL=https://your-domain.com/api

# NextAuth Configuration (required for authentication)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_key_change_in_production
ADMIN_PASSWORD=admin123

# Coolify Configuration
# These will be automatically set by Coolify:
# COOLIFY_FQDN=your-app.your-domain.com
# COOLIFY_URL=https://your-app.your-domain.com

# Development/Local Configuration (not needed for Coolify)
# PgAdmin Configuration (only for local development)
# PGADMIN_DEFAULT_EMAIL=admin@example.com
# PGADMIN_DEFAULT_PASSWORD=admin
